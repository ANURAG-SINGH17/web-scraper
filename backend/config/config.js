const config = {
    headless: true, // Set to false to see browser while debugging
    timeout: 30000, // Max time for page load
    outputFile: "./data/output.json", // Path to save scraped data
};

module.exports = config;
